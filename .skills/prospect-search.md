# Prospect Search Skill

Recipe: Given a category and location, run web searches (maps, local directories) to build a list of local businesses. Filter out national chains.

Outputs: `businesses` array matching prospector data contract.
